<h2>Users list</h2>
<% @users.each do |user| %>
	<p> <%= user.id %> : <%= user.name %>, Tasks created: <%= user.tasks.count %>  <%= link_to "Modifier", edit_admin_user_path(user.id) %> <%= link_to 'Supprimer', admin_user_path(user.id), method: :delete %> <%= link_to 'Show', admin_user_path(user.id) %></</td>
</p>
<% end %>